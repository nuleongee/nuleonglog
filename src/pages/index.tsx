import * as React from 'react';

import HeaderContainer from '../components/Base/HeaderContainer';
import BodyContainer from '../components/Base/BodyContainer';

// markup
const IndexPage = () => {
  return (
    <main>
      <HeaderContainer />
      <BodyContainer>Home</BodyContainer>
    </main>
  );
};

export default IndexPage;
